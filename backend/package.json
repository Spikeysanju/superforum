{
  "name": "superforum",
  "version": "1.0.1",
  "description": "A forum for the super",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --target=bun --outfile=server.js",
    "start": "bun src/index.ts"
  },
  "dependencies": {
    "hono": "^4.5.8"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}