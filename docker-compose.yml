services:
  backend:
    image: ghcr.io/spikeysanju/superforum-backend:main
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production

  frontend:
    image: ghcr.io/spikeysanju/superforum-frontend:main
    ports:
      - "3005:3005"
    environment:
      - NODE_ENV=production